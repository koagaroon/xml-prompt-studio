name: CI

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: Frontend quality (Node ${{ matrix.node }}, Windows)
    runs-on: windows-2025
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        include:
          - node: "22.13.0"
            check_latest: false
            audit: false
          - node: "24"
            check_latest: true
            audit: true
          - node: "26"
            check_latest: true
            audit: false

    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node }}
          check-latest: ${{ matrix.check_latest }}
          cache: npm

      - name: Use the declared npm version
        shell: pwsh
        run: |
          $npmVersion = node -p "require('./package.json').packageManager.split('@').at(-1)"
          npm install --global "npm@$npmVersion"
          if ((npm --version) -ne $npmVersion) {
            throw "Expected npm $npmVersion, got $(npm --version)"
          }

      - name: Install locked Node dependencies
        run: npm ci

      - name: Audit the complete Node dependency graph
        if: matrix.audit
        run: npm audit

      - name: Check TypeScript 7 release and TypeScript 6 API compatibility
        run: npm run typecheck:all

      - name: Lint JavaScript and formatting
        run: npm run lint:js

      - name: Lint CSS
        run: npm run lint:css

      - name: Run frontend tests
        run: npm run test:js

      - name: Build the production frontend
        run: npm run build

  rust:
    name: Rust quality (Windows)
    runs-on: windows-2025
    timeout-minutes: 120
    env:
      CARGO_TERM_COLOR: always
      RUST_BACKTRACE: "1"

    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          check-latest: true
          cache: npm

      - name: Use the declared npm version
        shell: pwsh
        run: |
          $npmVersion = node -p "require('./package.json').packageManager.split('@').at(-1)"
          npm install --global "npm@$npmVersion"
          if ((npm --version) -ne $npmVersion) {
            throw "Expected npm $npmVersion, got $(npm --version)"
          }

      - name: Cache Cargo downloads
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: ${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-cargo-

      - name: Keep the Cargo target on the Cargo registry drive
        shell: pwsh
        run: |
          $cargoHome = if ([string]::IsNullOrWhiteSpace($env:CARGO_HOME)) {
            Join-Path $env:USERPROFILE ".cargo"
          } else {
            $env:CARGO_HOME
          }
          $targetDir = Join-Path $cargoHome "xml-prompt-studio-target"
          "CARGO_TARGET_DIR=$targetDir" |
            Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Install locked Node dependencies
        run: npm ci

      - name: Install the pinned Rust toolchain
        shell: pwsh
        run: |
          rustup toolchain install --profile minimal --component rustfmt --component clippy --no-self-update
          rustc --version --verbose

      - name: Check Rust formatting
        run: cargo fmt --manifest-path src-tauri/Cargo.toml --check

      - name: Run Clippy on every Rust target
        run: cargo clippy --manifest-path src-tauri/Cargo.toml --locked --all-targets -- -D warnings

      - name: Run the locked Rust test suite
        run: cargo test --manifest-path src-tauri/Cargo.toml --locked

      - name: Smoke-test the Tauri build entry point
        run: npm run tauri build -- --debug --no-bundle --ci

  rust-msrv:
    name: Rust MSRV compatibility (Windows)
    runs-on: windows-2025
    timeout-minutes: 90
    env:
      CARGO_TERM_COLOR: always

    steps:
      - name: Check out the repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Cache Cargo downloads
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: ${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-cargo-

      - name: Keep the MSRV target on the Cargo registry drive
        shell: pwsh
        run: |
          $cargoHome = if ([string]::IsNullOrWhiteSpace($env:CARGO_HOME)) {
            Join-Path $env:USERPROFILE ".cargo"
          } else {
            $env:CARGO_HOME
          }
          $targetDir = Join-Path $cargoHome "xml-prompt-studio-msrv-target"
          "CARGO_TARGET_DIR=$targetDir" |
            Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Read the minimum supported Rust version
        shell: pwsh
        run: |
          $manifest = Get-Content "src-tauri/Cargo.toml" -Raw
          $match = [regex]::Match($manifest, '(?m)^rust-version\s*=\s*"(?<version>\d+\.\d+(?:\.\d+)?)"\s*$')
          if (-not $match.Success) {
            throw "src-tauri/Cargo.toml must declare a numeric rust-version"
          }
          $parts = $match.Groups["version"].Value.Split(".")
          $toolchain = if ($parts.Count -eq 2) {
            "$($parts[0]).$($parts[1]).0"
          } else {
            $match.Groups["version"].Value
          }
          "RUST_MSRV=$toolchain" |
            Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Install the minimum supported Rust toolchain
        shell: pwsh
        run: rustup toolchain install $env:RUST_MSRV --profile minimal --no-self-update

      - name: Check every target with the minimum supported Rust version
        shell: pwsh
        run: rustup run $env:RUST_MSRV cargo check --manifest-path src-tauri/Cargo.toml --locked --all-targets
